---
import { getCollection } from 'astro:content';
// import Section from './Section.astro';
import { ATLASSIAN_URL } from '../data/constants';

// const blogs = (await getCollection('blog')).map(({ data, id }) => {
//   const { title, description, author } = data;
//   return {
//     title,
//     description,
//     author,
//     link: `/blogs/${id}`,
//   };
// });

const projects = (await getCollection('projects')).map(({ data }) => {
  return data;
});
---

<main class="max-w-[620px] flex flex-col gap-12">
  <div class="flex flex-col gap-4 space-y-5">
    <h2 class="font-semibold">Today</h2>
    <p class="text-neutral-500 dark:text-neutral-400">
      I work as a software engineer at <a
        class="underline underline-offset-4 decoration-1 decoration-neutral-300 dark:hover:text-neutral-200 hover:text-black dark:decoration-neutral-500 transition-colors"
        href={ATLASSIAN_URL}>Atlassian</a
      >, where I'm building and maintaining microservices for the Admin
      Experience team.
    </p>
  </div>
  <!-- <Section title="Projects" anchorTarget="_blank" items={projects} /> -->
  <!-- <Section title="Writings" items={blogs} /> -->
</main>
